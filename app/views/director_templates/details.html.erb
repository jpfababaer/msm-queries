<h1>Director #<%= @the_director.id %> details</h1>

<a href="/directors">Go back</a>

<!--17.5 <hr> is a "thematic break". It basically creates a solid horizontal line. Helps break the webpage into section. -->
<hr>

<!--17.6 <dl> is a description list. Encloses a group of terms, <dt>, and the description for that ONE term, <dd>-->
<dl>
  <dt>Name</dt>
  <dd> <%=@the_director.name%> </dd>

  <dt>Dob</dt>
  <dd> <%= @the_director.dob%> </dd>

  <dt>Bio</dt>
  <dd> <%= @the_director.bio%> </dd>

  <dt>Image</dt>
  <dd> 
    <img src="<%= @the_director.image%> " width="">
  
  </dd>

  <dt>Created at</dt>
  <dd> <%= @the_director.created_at%> </dd>

  <dt>Updated at</dt>
  <dd> <%= @the_director.updated_at%> </dd>
</dl>

<!--18 To display the filmographies the Director's had done, we have to use the "foreign_key" (i.e. :director_id) to search for the matching id # INSIDE of the Movies database. -> SEARCH actor_templates/details--> 
<% matching_movies = Movie.where({ :director_id => @the_director.id })%>

<table border="1">
  <tr>
    <th>ID</th>
    <th>Image</th>
    <th>Title</th>
    <th>Year</th>
    <th>Director</th>
    <th>Show details</th>

  </tr>


<% matching_movies.each do |a_movie| %>
  <tr>
    <td><%= a_movie.id%></td>
    <td>
      <img src="<%= a_movie.image%>" width="50">
    
    </td>
    <td><%= a_movie.title%></td>
    <td><%= a_movie.year%></td>
    <td><%= a_movie.director_id%></td>
    <td>
      <a href="/movies/<%= a_movie.id%>">Show details</a>
    </td>
  <tr>
<%end%>

</table>
